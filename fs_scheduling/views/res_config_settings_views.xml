<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form_scheduling" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.scheduling</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="25"/>
        <field name="inherit_id" ref="fs_core.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//app[@name='fs_core']" position="inside">
                <block title="Scheduling Management" name="fs_scheduling_settings" groups="fs_core.group_flight_school_manager">
                    <setting string="Callsign Prefix" help="Default prefix for flight mission callsigns (e.g. ABS)">
                        <field name="fs_mission_callsign_prefix"/>
                    </setting>
                    <setting string="First Added Mission Number" help="Threshold for added missions. Regular missions use numbers below this value, resetting yearly.">
                        <field name="fs_first_added_mission_number"/>
                    </setting>
                    <setting string="Buffer Minutes" help="Buffer time in minutes between consecutive missions">
                        <field name="fs_scheduling_buffer_minutes"/>
                    </setting>
                    <setting string="Time Slot Granularity" help="Minimum time slot increment for scheduling">
                        <field name="fs_scheduling_time_slot_minutes"/>
                    </setting>
                </block>
            </xpath>
        </field>
    </record>

    <record id="action_fs_scheduling_config_settings" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">current</field>
        <field name="context">{'module': 'fs_scheduling', 'bin_size': False}</field>
    </record>
</odoo>
